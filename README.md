# AVDKit-Release
AVDKit is a cross-platform desktop control center that bootstraps the Android SDK, manages virtual devices, installs APKs, and automates common emulator workflows without requiring Android Studio
